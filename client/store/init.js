const defaultState = {
  phone: '860.305.0071',
  company: 'Westchester Marketplace',

};

export default function(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
